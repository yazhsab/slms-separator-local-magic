# Worked Certificate Files

This directory indexes the worked certificate data used by Appendix G.

Primary files:

- `../appendices/G_worked_certificate.tex`
- `../results/worked_certificate_recurrence.csv`
- `../results/slms_certificate_results.csv`

The worked certificate is deliberately a fallback lambeq-compatible structured instance, not a parser-generated lambeq resource certificate.
